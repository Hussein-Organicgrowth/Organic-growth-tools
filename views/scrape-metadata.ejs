<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Other meta tags, styles and scripts here -->
    <title>Sitemap Meta Scraper</title>
    <style>
      .container {
        max-width: 800px;
        margin: 0 auto;
        padding: 20px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      }

      h1 {
        text-align: center;
        margin-bottom: 20px;
      }

      #sitemapUrl {
        width: 100%;
        padding: 10px;
        margin-bottom: 20px;
      }

      button {
        display: block;
        margin: 0 auto;
        padding: 10px 20px;
        background-color: #007bff;
        color: white;
        border: none;
        cursor: pointer;
        border-radius: 4px;
      }

      .meta-item {
        border: 1px solid #ccc;
        padding: 10px;
        margin-top: 20px;
      }
      table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 20px;
      }

      th,
      td {
        padding: 10px;
        border: 1px solid #ccc;
      }

      th {
        background-color: #f4f4f4;
        text-align: left;
      }

      tbody tr:nth-child(even) {
        background-color: #f9f9f9;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Enter Sitemap URL</h1>
      <input
        type="url"
        id="sitemapUrl"
        placeholder="https://example.com/sitemap.xml" />
      <button onclick="fetchUrls()">Get Meta Data</button>
    </div>
    <div id="result">
      <table>
        <thead>
          <tr>
            <th>URL</th>
            <th>Title</th>
            <th>Description</th>
          </tr>
        </thead>
        <tbody id="tableBody">
          <!-- Rows will be appended here -->
        </tbody>
      </table>
    </div>
    <script src="/js/meta-scraper.js"></script>
  </body>
</html>
